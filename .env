# Server Configuration
PORT=5000
JWT_SECRET=portfolio_jwt_secret_token_change_in_production_123!

# Database Configuration (Optional)
# If left blank, the application will use the local JSON file database fallback automatically.
MONGODB_URI=

# Initial Admin Credentials (Created on first startup if no admin exists)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=password123
